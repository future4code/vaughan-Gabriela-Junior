export type User = {
    id: number | string,
    name: string,
    email: string,
    password: string
};

export type Product = {
    id: string,
    name: string,
    price: number,
    image_url: string
};